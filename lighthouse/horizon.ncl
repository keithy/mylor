# Horizon Configuration - What to watch and build
# This file defines the pipelines and their source configurations

{
  pipelines = [
    # Example pipeline configurations would go here
    # Each pipeline defines what repos/branches/files to watch
    # and when they should be built (nightly, weekly, on-change, etc.)
  ]
}